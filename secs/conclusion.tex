\vspace{-2mm}
\section{Conclusion and Discussion}
\vspace{-2mm}
\label{sec:conclusion}
We present automatic neural baking, a representation that composes directly with the traditional
rendering pipeline: an element-wise ``texture'' whose domain is the mesh surface and whose blending
is order-independent, so the asset needs no UV atlas and no depth sort and is evaluated per fragment
at real-time rates. A 2D Gaussian mixture anchored in barycentric coordinates carries the
view-independent fields, an adaptive hierarchical environment map the incident light, and a FastKAN
decoder the view-dependent term. With this bridge in place, the next steps are engine
integration, real captures, and element-level operations such as merging or editing disks directly.

\paragraph{Scope.} Our method takes the mesh as input, is optimized per scene, and is evaluated on
synthetic single-object benchmarks. Within a near-convex part, light from the rest of the object is treated as distant and position-independent, so strongly concave objects are the hardest
case: \emph{toaster} has our lowest absolute PSNR, even though we still lead every context method
there (Table~\ref{tab:shiny_perscene}). Real-time evaluation is demonstrated in
nvdiffrast rather than a shipped shader. We evaluate with the provided meshes; behavior on
reconstructed meshes is not studied in our main experiments. Because the anchoring is linear in the vertices, appearance gradients also reach the mesh; App.~\ref{app:refinement} uses this property to refine a reconstructed mesh as a demonstration, and a full study of refinement is left to future work.

% mandatory not included in page limit
\section{AI Disclosure}
In this work, we used generative AI tools to assist with writing and editing the language of the paper, including grammar and LaTeX formatting. The ideas of the method were developed by the authors. We used generative AI tools (e.g., Claude Code) in an assistive capacity during code implementation, including evaluation scripts, visualization scripts, and debugging; generative AI tools also assisted in implementing the proposed method. For data cleaning and reformatting, generative AI tools assisted in writing preprocessing and dataloader code; they were not used to edit or generate the data itself. We also used generative AI tools to assist in developing theoretical models and conceptual frameworks, formulating mathematical claims, and writing proofs, including providing critical ingredients for the proofs.

We have not used generative AI tools to generate synthetic datasets, to propose or refine hypotheses, to design or provide feedback on research methodology or experiments, or to translate. Research direction, qualitative analysis, interpretation of results, and experiment design are the authors' own.

The authors have reviewed all AI-assisted work. All AI-assisted proofs and mathematical claims were checked by the authors, and AI-assisted code was tested for correctness. 

We take full responsibility for the final content of this work, including text, claims, and artifacts produced with the aid of generative AI.


\section{Reproducibility}
All datasets used, NeRF-Synthetic~\citep{NeRFVanilla} and Shiny-Blender~\citep{refnerf}, are public
benchmarks with standard splits. The training objective
(Eq.~\eqref{eq:loss}, Adam optimizer, 30K iterations) is given in Sec.~\ref{sec:overview} and the mixture's hyperparameters in Sec.~\ref{sec:gmm}; the convex
decomposition and adaptive environment-map budget in Sec.~\ref{sec:env}; and the FastKAN decoder
architecture in Sec.~\ref{sec:kan}. We will release our full codebase upon the paper's acceptance or
publication.

